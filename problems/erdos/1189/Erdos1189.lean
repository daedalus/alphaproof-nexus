import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 1189

*Reference:* https://www.erdosproblems.com/1189

Call a set of distinct integers $1How many irreducible covering sets of size $k$ are there?What is the minimum and maximum that $n_k$ can be?Determine or estimate $\max \sum\frac{1}{n_i}$, where the maximum ranges over all irreducible covering sets of size $k$.Are there infinitely many $n$ such that the divisors of $n$ (which are $>1$) form an irreducible covering set?
-/

namespace Erdos1189

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos1189
