import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 1101

*Reference:* https://www.erdosproblems.com/1101

If $u=\{u_10$, if $x$ is sufficiently large then\[\max_{a_k<x} (a_{k+1}-a_k) < (1+\epsilon)t_x \prod_{i}\left(1-\frac{1}{u_i}\right)^{-1}.\]Is there a good sequence such that $u_n< n^{O(1)}$? Is there a good sequence such that $u_n\leq e^{o(n)}$?
-/

namespace Erdos1101

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos1101
