import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 1100

*Reference:* https://www.erdosproblems.com/1100

If $1=d_1Is it true that $\tau_\perp(n)/\omega(n)\to \infty$ for almost all $n$? Is it true that\[\tau_\perp(n)Let\[g(k) = \max_{\omega(n)=k}\tau_\perp(n),\]where $\omega(n)$ counts the number of distinct prime divisors of $n$, and $n$ is restricted to squarefree integers. Determine the growth of $g(k)$.
-/

namespace Erdos1100

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos1100
