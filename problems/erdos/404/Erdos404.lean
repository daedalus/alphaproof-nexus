import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 404

*Reference:* https://www.erdosproblems.com/404

For which integers $a\geq 1$ and primes $p$ is there a finite upper bound on those $k$ such that there are $a=a_1Is there a prime $p$ and an infinite sequence $a_1<a_2<\cdots$ such that if $p^{m_k}$ is the highest power of $p$ dividing $\sum_{i\leq k}a_i!$ then $m_k\to \infty$?
-/

namespace Erdos404

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos404
