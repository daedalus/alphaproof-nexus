import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 855

*Reference:* https://www.erdosproblems.com/855

If $\pi(x)$ counts the number of primes in $[1,x]$ then is it true that (for large $x$ and $y$)\[\pi(x+y) \leq \pi(x)+\pi(y)?\]
-/

namespace Erdos855

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos855
