import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 18

*Reference:* https://www.erdosproblems.com/18

We call $m$ practical if every integer $1\leq nAre there infinitely many practical $m$ such that\[h(m) < (\log\log m)^{O(1)}?\]Is it true that $h(n!)<n^{o(1)}$? Or perhaps even $h(n!)<(\log n)^{O(1)}$?
-/

namespace Erdos18

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos18
