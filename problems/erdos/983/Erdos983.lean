import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 983

*Reference:* https://www.erdosproblems.com/983

Let $n\geq 2$ and $\pi(n)r$ many $a\in A$ are only divisible by primes from $\{p_1,\ldots,p_r\}$. Is it true that\[2\pi(n^{1/2})-f(\pi(n)+1,n)\to \infty\]as $n\to \infty$?In general, estimate $f(k,n)$, particularly when $\pi(n)+1<k=o(n)$.
-/

namespace Erdos983

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos983
