import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 196

*Reference:* https://www.erdosproblems.com/196

Must every permutation of $\mathbb{N}$ contain a monotone 4-term arithmetic progression? In other words, given a permutation $x$ of $\mathbb{N}$ must there be indices with either $ij>k>l$ such that $x_i,x_j,x_k,x_l$ are an arithmetic progression?
-/

namespace Erdos196

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos196
