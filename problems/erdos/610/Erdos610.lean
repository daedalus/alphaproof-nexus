import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 610

*Reference:* https://www.erdosproblems.com/610

For a graph $G$ let $\tau(G)$ denote the minimal number of vertices that include at least one from each maximal clique of $G$ (sometimes called the clique transversal number).Estimate $\tau(G)$. In particular, is it true that if $G$ has $n$ vertices then\[\tau(G) \leq n-\omega(n)\sqrt{n}\]for some $\omega(n)\to \infty$, or even\[\tau(G) \leq n-c\sqrt{n\log n}\]for some absolute constant $c>0$?
-/

namespace Erdos610

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos610
