import Mathlib

open Set Finset
open scoped BigOperators

/-!
# Erdős Problem 501

*Reference:* https://www.erdosproblems.com/501

For every $x\in\mathbb{R}$ let $A_x\subset \mathbb{R}$ be a bounded set with outer measure $If the sets $A_x$ are closed and have measure $<1$, then must there exist an independent set of size $3$?
-/

namespace Erdos501

-- EVOLVE-BLOCK-START
-- EVOLVE-BLOCK-END

end Erdos501
